# UiFonts

UiFonts contains the shared UI font assets and a small startup workaround that preloads the main TMP font before Addressable UI is shown.
It fixes a runtime issue observed on mobile devices, where text can lose its shared material after the UI appears.
Add all fonts used by your Addressables here.
